# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.*
!.env.example

# Node
node_modules/
dist/
.vite/
coverage/
*.tsbuildinfo

# RL checkpoints
algorithms/rl/checkpoints/

# IPPO generated experiment artifacts
algorithms/ippo/runs/
algorithms/ippo/checkpoints/
algorithms/ippo/*.pt

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Logs, outputs, and local verification artifacts
*.log
outputs/

# Keep official SUMO sources tracked (required at runtime after refactor).
# Ignore only local scratch under official/, not the committed map/tls/traffic trees.
data/maps/sumo/official/**/*.bak
data/maps/sumo/official/**/*.tmp
*.bak

# Local run artifacts & backups (kept on disk, not committed)
.codex-backups/
algorithms/coslight/runs/
algorithms/ippo/runs/
algorithms/ippo/checkpoints/

# Frontend UI capture artifacts
current-ui*.png
historical-ui-contact-sheet.jpg
assets/preview/

# Local browser and verification artifacts
.playwright-cli/
_sid.txt
output/
# IPPO regression/gate local artifacts (not committed)
algorithms/ippo/regression_tmp/
algorithms/ippo/gate_outputs/

# Local rollback backups (not committed)
.codex-backups/

# Experiment logs (remote scratch)
logs/

# M1/cooperative run artifacts (kept on disk, not committed)
algorithms/mappo/runs/
# Editor/merge backups
*.orig
/runs/

# CARLA Maps
data/maps/carla/*
data/maps/xodr/*

# CARLA roadside camera frames and encoded MP4s (local media, never commit)
roadside_media/demo_*/
roadside_media/encoded/*.mp4
roadside_media/encoded/*.concat.txt
roadside_media/*.json
roadside_media/*.jsonl
roadside_media/**/manifest.jsonl
!roadside_media/README.md

# vibe coding
.claude

# prediction
official20_lane206_prediction_backend_bundle_20260810/

# Copilot local configuration and service runtime (model paths stay machine-local)
scripts/copilot/copilot.env
outputs/.copilot_runtime/
